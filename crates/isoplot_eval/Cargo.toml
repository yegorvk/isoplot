[package]
name = "isoplot-eval"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
logos = "0.16.1"
thiserror.workspace = true

[dependencies.bytemuck]
workspace = true
features = ["derive", "min_const_generics", "must_cast"]

[dependencies.cranelift]
version = "0.134.2"
optional = true
features = ["jit", "module", "native"]

[features]
default = ["default-fallback"]
cranelift = ["dep:cranelift"]
default-fallback = []
default-cranelift = ["cranelift"]
